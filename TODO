 - force'ing to a non-existent alias should fail.
 - packages can disappear
 - rm -rf debian/ from tarballs?
 - geneland dependency failure in apt *not* cran2deb
 - fine grained bad license breakdown
